# 学術ポスターテンプレート 制作仕様書

`poster_template.html` に基づく制約の明文化．  
**可変項目**はサイズ・向き・段組み・カラーパレットのみ．  
それ以外（フォント・余白・角丸・配置ルール）は**固定**とし，勝手に変更しない．
フォントサイズも原則固定だが，本文サイズのみA0可読性・1ページ収まりの理由がある場合に限り §3-2 の範囲内で調整できる．

---

## 1. ファイル構成

| ファイル | 役割 |
|---|---|
| `poster_template.html` | ポスター本体（HTML+CSS+JS 単一ファイル） |
| `poster_spec.md` | 本仕様書 |

ポスターはHTMLファイル単体で完結する．外部CSSファイルは使わない．  
印刷はChrome推奨（用紙：指定サイズ，余白：なし，背景グラフィック：オン）．

---

## 2. 可変項目（発表先・用途に応じて変更してよい）

### 2-1. ポスターサイズと向き

`:root` の `--poster-w` と `--poster-h`，および `@media print` の `@page { size: ... }` を同時に書き換える．

| 用途 | `--poster-w` | `--poster-h` | `@page size` |
|---|---|---|---|
| **A0縦（標準）** | `841mm` | `1189mm` | `841mm 1189mm` |
| A0横 | `1189mm` | `841mm` | `1189mm 841mm` |
| A1縦 | `594mm` | `841mm` | `594mm 841mm` |

`@page size` の値は `--poster-w --poster-h` と必ず一致させること．  
JSの `MM_W` / `MM_H` は `:root` の `--poster-w` / `--poster-h` を自動で読むため，**手動で書き換える必要はない**．  
したがってサイズ変更時に手で直すのは **`:root` の2変数と `@page size` の2箇所のみ**（一致は `tools/lint.mjs` が検証する）．

### 2-2. 段組み

セクションカード `.sc` の内部でのみ段組みを使う．  
本文全体の段数は**1段組みを基本**とする（`.pb` は縦積みflex固定）．  
カード内に限り以下の分割クラスを使ってよい：

| クラス | 構成比 | 用途例 |
|---|---|---|
| `.cols-2` | 1:1 | 図と説明文の並列 |
| `.cols-3` | 1:1:1 | 指標カードの3列並び |
| `.cols-2-1` | 2:1 | 本文＋参考文献 |
| `.cols-1-2` | 1:2 | 箇条書き＋強調ボックス |

段組みの gap は `6mm` 固定．変更不可．

### 2-3. カラーパレット

`:root` の以下の変数のみ変更してよい．その他の色変数は変更不可．

```css
/* 変更可能な2変数 */
--col-primary:   #502F8E;   /* 主色（ヘッダー帯・見出し帯・フッター帯） */
--col-secondary: #F97213;   /* アクセント（番号丸・強調罫線・タグ） */
```

**学会別プリセット例：**

```css
/* プリセット例A：紫×オレンジ */
--col-primary:   #502F8E;   /* 機関・学会のブランドカラーに差し替える */
--col-secondary: #F97213;   /* アクセントカラーに差し替える */

/* プリセット例B：オレンジ×紫（反転） */
--col-primary:   #E8600A;
--col-secondary: #502F8E;
```

補助カラー（データや図中の色分けに使う，テンプレート構造には使用しない）：

```css
--col-red:    #D4333D;
--col-blue:   #0171BE;
--col-yellow: #F3AB26;
--col-teal:   #287E88;
```

**新規カラー変数は `oklch()` で定義する（推奨）：**

既存プリセット（上記の hex）はそのままでよい．**新しく**色変数を足すときは hex/rgba ではなく
`oklch(L C H)` で書く．oklch は知覚均一なので，明度 `L` を揃えれば複数色の輝度が揃い，印刷時の
見えやすさ調整が直感的になる（hex だと輝度を別途計算する必要がある）．特にデータ色分け用の tint を
複数作るときは，`L` と `C` を固定して `H`（色相）だけ変えると，**輝度の揃った色相セット**が一発で得られる．

```css
/* 例：L=0.62 で輝度を揃えた tint セット（青・緑・橙） */
--col-blue-tint:   oklch(0.62 0.10 250);
--col-teal-tint:   oklch(0.62 0.10 175);
--col-orange-tint: oklch(0.62 0.10  55);
```

- **アルファ付き（`rgba(...)` や `oklch(... / α)`）は印刷で背景と合成され色が転ぶことがある．**
  色分け用 tint は必ず**不透明色**で持つ（半透明を実色に焼き込む）．
- Chrome headless（112+）の PDF 出力で表示・印刷とも確認済み．既存 hex の一括置換は不要．

---

## 3. 固定項目（変更禁止）

### 3-1. フォント

**変更禁止．代替フォントの追加も不可．**

| 役割 | 指定フォント | 使用箇所 |
|---|---|---|
| 見出し（display） | Noto Serif JP → Georgia → serif | タイトル・セクション見出し |
| 本文（body） | Noto Sans JP → Hiragino Sans → 游ゴシック → sans-serif | すべての本文・補足・キャプション |

游ゴシック以下はローカルフォールバック．

**フォントの配信方法（重要）：** 編集・プレビュー時は Google Fonts（Noto）のオンライン自動読み込みでよい．
ただし**印刷用の最終成果物は，使用字種をサブセット化したローカル woff2 を `@font-face` で埋め込むことを推奨する**．
印刷PC・印刷所・オフライン環境ではオンライン読み込みが効かず，無言で別フォントに化けるリスクがあるため．
サブセット化と埋め込みの手順は `docs/FONT_EMBEDDING.md` を参照．埋め込み後は，テンプレ同梱の
**フォント未読込検知バナー（`#font-warning` スクリプト）**で適用漏れに気づけるようにしておく．

学術ポスターは見出し Noto Serif JP・本文 Noto Sans JP の**全字種**を使うため，装飾用フォントより
サブセット範囲が広く，ファイルサイズも大きくなる（本文用 Noto Sans JP だけで数MB規模）点に注意．

### 3-2. フォントサイズ（pt単位固定）

A0実寸・1–2m閲覧距離を前提とした値．HTML側で `font-size` を直書きしない．
本文サイズを既定から変える場合は，理由を `docs/DECISIONS.md` または `docs/PROJECT_LOG.md` に記録する．

| 変数 | サイズ | 使用箇所 |
|---|---|---|
| `--fs-title` | **90pt** | ポスタータイトル |
| `--fs-authors` | **48pt** | 著者名 |
| `--fs-affil` | **36pt** | 所属・連絡先 |
| `--fs-tag` | **28pt** | カンファレンスタグ・ラベル |
| `--fs-heading` | **48pt** | セクション見出し |
| `--fs-body` | **32pt**（許容範囲 28–36pt） | 本文・箇条書き |
| `--fs-sub` | **26pt** | 補足本文・小見出し・参考文献見出し |
| `--fs-caption` | **24pt** | 図キャプション・フッター・小ラベル |
| `--fs-refs` | **22pt** | 参考文献リスト（唯一の例外的小サイズ） |

**24pt未満は使用禁止**（`--fs-refs` の22ptのみ例外）．`tools/lint.mjs` は最小値と主要変数の範囲を検査する．

### 3-3. 余白（mm単位固定）

| 変数 | サイズ | 使用箇所 |
|---|---|---|
| `--pad-poster` | **10mm** | ポスター左右・上下の外周余白 |
| `--pad-card` | **5mm** | セクションカード内側余白（全辺） |
| `--pad-chip` | **2.5mm** | タグ・小ラベルの内側余白（上下） |
| `--gap-block` | **8mm** | セクションカード間の縦隙間 |

### 3-4. 角丸（mm実寸固定）

**px指定への変換・比率指定への変更は禁止．**

| 変数 | サイズ | 適用箇所 |
|---|---|---|
| `--r-card` | **5mm** | セクションカード `.sc`，強調ボックス `.hbox`，図枠 `.fig` |
| `--r-wide` | **10mm** | 幅の広い背景カード（全幅装飾ブロックに使う場合） |
| `--r-chip` | **2mm** | タグ `.ph-tag`，小ラベル，QRコード枠 |

**角丸なし（`border-radius: 0`）の要素（固定）：**
- ヘッダー帯 `.ph`
- セクション見出し帯 `.sc-title`
- フッター帯 `.pf`
- 強調ボックスの左罫線側（`.hbox` の左辺）

### 3-5. テキスト配置

| 対象 | 配置 |
|---|---|
| すべての本文・箇条書き | **左揃え**（`text-align: left`） |
| タイトル（`.ph-title`） | 左揃え（ヘッダー帯内） |
| セクション番号 `.n` | 中央揃え（丸バッジ内のみ） |
| QRプレースホルダー `.pf-qr` | 中央揃え（QR枠内のみ） |
| フッター助成情報 `.pf-fund` | 右揃え |
| キャプション `.fc` / `.fig-cap` | 中央揃え（図枠内下部のみ） |

本文・見出し帯・補足テキストへの `text-align: center` の追加は禁止．

### 3-6. 罫線・ボーダー

| 要素 | 仕様 |
|---|---|
| セクションカード外枠 | `0.4mm solid` `--col-border`（= `rgba(--col-primary, 0.15)`） |
| 強調ボックス左罫 `.hbox` | `1.5mm solid --col-secondary` |
| 強調ボックス左罫 `.hbox-primary` | `1.5mm solid --col-primary` |
| 強調ボックス左罫 `.hbox-teal` | `1.5mm solid --col-teal` |
| ロゴプレースホルダー枠 | `0.5mm solid rgba(255,255,255,0.30)` |
| タグ枠 | `0.4mm solid rgba(255,255,255,0.28)` |
| 図プレースホルダー枠 | `0.5mm dashed rgba(--col-primary, 0.25)` |

---

## 4. HTML構造ルール

### 4-1. 必須構造（変更不可）

```
#scale-wrap
└── #poster  (display: flex; flex-direction: column; min-height: var(--poster-h))
    ├── <header class="ph">          ヘッダー帯
    ├── <div class="poster-grow">   flex: 1（フッターを最下部に押し下げる）
    │   └── <div class="pb">        セクション縦積みコンテナ
    │       ├── <section class="sc"> セクション1
    │       ├── <section class="sc"> セクション2
    │       └── ...
    └── <footer class="pf">         フッター帯（常にポスター最下部）
```

### 4-2. セクションカードの構成ルール

```html
<section class="sc">
  <h2 class="sc-title">
    <span class="n">番号</span>セクション名
  </h2>
  <!-- 本文コンテンツ -->
</section>
```

- すべてのセクションは `.sc` クラスを持つこと
- 見出しは必ず `<h2 class="sc-title">` を使うこと（`<h3>` 以下への格下げ禁止）
- 番号バッジ `<span class="n">` は省略可能だが省略時は番号なしになる

### 4-3. 強調ボックスの使い分け

| クラス | 配色 | 用途 |
|---|---|---|
| `.hbox` | オレンジ左罫・薄オレンジ背景 | 問題の核心，持ち帰りメッセージ |
| `.hbox-primary` | 紫左罫・薄紫背景 | 仮説，定義，理論的根拠 |
| `.hbox-teal` | 緑左罫・薄緑背景 | 補助色tealに対応する意味方向・対比 |

条件比較や実験設定の説明では，縦に強調ボックスを積みすぎない．操作・役割・値を並べたい場合は `.cond-table` を優先する．

### 4-4. 図・グラフの記述方法

**プレースホルダー（作業中）：**
```html
<div class="fig" style="height: ○○mm">
  <div class="fi">🖼</div>
  <div class="fn">図の内容説明</div>
  <div class="fc">Fig. N — キャプション</div>
</div>
```

**実画像を入れる場合：**
```html
<div class="fig-wrap">
  <img src="figure.png" alt="図の説明">
  <div class="fig-cap">Fig. N — キャプション</div>
</div>
```

`style="height: ○○mm"` はプレースホルダー専用．実画像では不要．

**複数結果図の横並び：**
`.cols-3.result-figs` のように `.result-figs` を併用すると，図ごとのキャプションが枠の下端に揃う．

**HTML/CSS概念図：**
最終組版で調整が必要な概念図は，外部画像ではなく `.chain-fig` / `.chain-list` / `.chain-card` / `.chain-arrow` を使ってよい．
名詞→特徴→名詞のような短いチェインを，本文フォント変数・配色変数に追従したまま表現できる．

### 4-5. 禁止事項（HTML側）

- `font-size` の直書き禁止（`--fs-*` 変数経由のみ）
- `color` の直書き禁止（`--col-*` 変数経由のみ）
- `border-radius` の直書き禁止（`--r-*` 変数経由のみ，またはコンポーネント固有の `0`）
- `text-align: center` を本文・補足・見出し帯に追加禁止
- `font-family` の直書き禁止（`--font-body` 変数経由のみ）

---

## 5. プレビューとスケール

ブラウザでHTMLを開くと，JSがウィンドウ幅に合わせてポスターを等比縮小表示する．  
ポスター本体のmm寸法は変わらない．ウィンドウをリサイズしても縦横比は維持される．

**スケールJSは `:root` の `--poster-w` / `--poster-h` を自動取得する**ため，サイズ変更時にJSを触る必要はない．
```js
var MM_W = readMM('--poster-w');  // "841mm" -> 841（自動）
var MM_H = readMM('--poster-h');
```

---

## 6. 印刷手順

1. Google Chrome でHTMLファイルを開く
2. `Ctrl+P`（`Cmd+P`）で印刷ダイアログを開く
3. 以下を設定する：
   - 送信先：PDFに保存（または対応プリンター）
   - 用紙サイズ：A0（または `--poster-w × --poster-h` に対応するサイズ）
   - 余白：**なし**
   - 背景のグラフィック：**オン**
4. 保存・印刷

Firefox / Safari は `@page size` の対応が不完全な場合があるため非推奨．

### 6-1. 印刷の落とし穴（画面とPDFでページ数が食い違う）

画面表示やPNGが1ページに見えても，**印刷PDFだけページが増える**ことがある．典型的な原因は，画面用に
与えた指定（段間マージン等）の詳細度が `@media print` 側の打ち消し（例 `margin:0`）より高くて残り，
各要素の `overflow:hidden` のせいで画面上はあふれが「隠れて」正しく見えているだけ，というパターン．

**対策：画面表示を信用せず，必ず `node tools/render.mjs` でPDFを生成してページ数を実測する**
（render はページ数が2以上なら警告して exit 1 する）．画面用と印刷用で差を出す指定を書くときは，
`@media print` 側の打ち消しが**詳細度負けしていないか**を確認すること．

---

## 7. バージョン管理メモ

| 項目 | 値 |
|---|---|
| ベーステンプレートバージョン | 1.0 |
| 最終更新 | 2025年 |
| 対応学会（デフォルト） | 特定学会に依存しない汎用テンプレート |
| デフォルトサイズ | A0縦（841mm × 1189mm） |
| デフォルト段組み | 1段組み（カード内部のみ複数列可） |
